apiVersion: v1
kind: Service
metadata:
    annotations:
        service.beta.kubernetes.io/azure-dns-label-name: bets
    name: notfightclubnotfightclubbets
spec:
    type: LoadBalancer
    ports:
    - port: 5007
    selector:
        app: notfightclubnotfightclubbets